interface:
  display_name: "Web App Security Hardening"
  short_description: "Audit and harden web apps with reproducible evidence"
  default_prompt: "Use $webapp-security-hardening to audit this web app, separate confirmed findings from leads, and produce a prioritized retest plan."
policy:
  allow_implicit_invocation: true
